# V9.1 release consistency

The public Python engine is V9.1. The package version is 0.9.1. `MODEL_STATE.json` is the evidence/measurement state schema and may carry a separate model-state identifier.
